"""Autoria de workflow: compor uma cascata a partir do que os domínios oferecem.

`PERMITIDO["authoring"]` é `{kernel, runtime, domains, agent, human}` — é a
camada que pode conhecer motor E blocos, e por isso é onde a composição mora.
"""
